# CPU-only PyTorch wheels (no CUDA/cuDNN/NCCL ~3GB pull on Linux)
--extra-index-url https://download.pytorch.org/whl/cpu

# Web UI
streamlit==1.39.0

# Data fetching
requests==2.32.3
yfinance==0.2.65
curl_cffi==0.7.4   # yfinance auto-detects and uses this to bypass Yahoo bot blocks

# Charts
plotly==5.24.1

# Kronos model runtime
torch==2.4.1+cpu
huggingface_hub==0.26.2
einops==0.8.0
safetensors==0.4.5
tqdm==4.66.5

# Numerical / data
pandas==2.2.3
numpy==1.26.4
